import React from 'react';
import { motion } from 'framer-motion';
import { fadeIn } from '../variants';

const Contact = () => {
  return (
  <  className='section' id='contact'>Contact</>
  );
};

export default Contact;
